apiVersion: 1

providers:
  - name: expense-tracker-v2-traces
    orgId: 1
    folder: 'Expense Tracker — v2 Traces'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
